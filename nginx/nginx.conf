events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;
    client_max_body_size 110M;

    # para WebSockets
    map $http_upgrade $connection_upgrade { default upgrade; '' close; }

    upstream backend {
        server backend:80;
    }

    upstream frontend {
        server frontend:80;
    }

    # ==> ADICIONA: upstream para o NPM (porta interna 80 do container)
    upstream npm {
        server nginx-proxy-manager-app-1:80;
    }

    # ====== TEU SITE (como já tinhas) ======
    server {
        listen 80;
        server_name localhost;

        location / {
            include /etc/nginx/mime.types; # opcional; só para mostrar que não mexemos
            proxy_pass http://frontend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
        }

        location /api/ {
            proxy_pass http://backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
        }
    }

    server {
        listen 80;
        server_name proxy.muaiadhadad.me;

        # Deixa o NPM tratar do desafio Let's Encrypt
        location ^~ /.well-known/acme-challenge/ {
            proxy_set_header Host $host;
            proxy_pass http://npm;
        }

        # Resto vai para o NPM (que depois reencaminha para 127.0.0.1:81 no próprio painel)
        location / {
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;

            proxy_pass http://npm;
        }
    }
}
